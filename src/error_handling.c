/*
** EPITECH PROJECT, 2022
** error_handling
** File description:
** error
*/

#include "my.h"
/*
int open_map_error(bsq_t *bs, char const *filepath)
{
    struct stat st;
    int res = stat(filepath, &st);

       if (bs->size == -1)
        return 84;
    if (bs->fd == -1)
        return 84;
    if (bs->file == -1)
        return 84;
    if (bs->res == -1)
    return 84;
    }*/
